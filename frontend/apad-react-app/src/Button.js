const Button = () => {

    const handleClick = () => {
        /* insert desired button clicked behavior here */
    }

    return (
        <button onClick={handleClick}>
            insert button text here 
            </button>
    )
}

export default Button;